---
import AdminLayout from "../../layouts/AdminLayout.astro";
import { supabase, supabaseAdmin } from "../../lib/supabase";

// Fetch Shops
const { data: shops, error: shopsError } = await supabaseAdmin
    .from("shops")
    .select("*, profile:profiles(email, full_name)")
    .order("created_at", { ascending: false });

if (shopsError) {
    console.error("Error fetching shops:", shopsError);
}

const pendingShops = shops?.filter((s) => s.status === "pending") || [];
const activeShops = shops?.filter((s) => s.status === "active") || [];
const suspendedShops = shops?.filter((s) => s.status === "suspended") || [];
---

<AdminLayout title="Seller Moderation">
    <div class="space-y-8">
        <div>
            <h1 class="text-3xl font-black text-white uppercase tracking-wider">
                Seller Moderation
            </h1>
            <p class="text-white/50 text-sm mt-1">
                Manage shop approvals and bans
            </p>
        </div>

        {/* Pending Requests */}
        <section
            class="bg-[#0A0A0A] border border-orange-500/30 rounded-xl p-6 relative overflow-hidden"
        >
            <div class="absolute top-0 right-0 p-4 opacity-10">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="w-32 h-32 text-orange-500"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                    ><path
                        d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"
                    ></path></svg
                >
            </div>

            <h2
                class="text-xl font-bold text-orange-500 mb-6 flex items-center gap-2 relative z-10"
            >
                <span class="w-2 h-2 rounded-full bg-orange-500 animate-pulse"
                ></span>
                Pending Approval ({pendingShops.length})
            </h2>

            <div class="grid gap-4 relative z-10">
                {
                    pendingShops.length === 0 ? (
                        <p class="text-white/30 italic">No pending requests.</p>
                    ) : (
                        pendingShops.map((shop) => (
                            <div class="bg-white/5 border border-white/10 rounded-lg p-4 flex flex-col md:flex-row items-center justify-between gap-4">
                                <div class="flex items-center gap-4">
                                    <div class="w-12 h-12 rounded-full bg-[#111] flex items-center justify-center border border-white/10">
                                        {shop.logo_url ? (
                                            <img
                                                src={shop.logo_url}
                                                class="w-full h-full rounded-full object-cover"
                                            />
                                        ) : (
                                            <span class="text-white/50">
                                                {shop.name[0]}
                                            </span>
                                        )}
                                    </div>
                                    <div>
                                        <h3 class="font-bold text-white text-lg">
                                            {shop.name}
                                        </h3>
                                        <p class="text-white/50 text-xs">
                                            Owner: {shop.profile?.full_name} (
                                            {shop.profile?.email})
                                        </p>
                                        <p class="text-white/50 text-xs">
                                            Slug:{" "}
                                            <span class="text-[#D4AF37]">
                                                @{shop.slug}
                                            </span>
                                        </p>
                                    </div>
                                </div>
                                <div class="flex gap-2">
                                    <button
                                        data-id={shop.id}
                                        data-action="approve"
                                        class="action-btn px-4 py-2 bg-green-500/20 text-green-400 border border-green-500/50 rounded-lg font-bold text-xs uppercase hover:bg-green-500 hover:text-black transition-colors"
                                    >
                                        Approve
                                    </button>
                                    <button
                                        data-id={shop.id}
                                        data-action="reject"
                                        class="action-btn px-4 py-2 bg-red-500/20 text-red-400 border border-red-500/50 rounded-lg font-bold text-xs uppercase hover:bg-red-500 hover:text-black transition-colors"
                                    >
                                        Reject
                                    </button>
                                </div>
                            </div>
                        ))
                    )
                }
            </div>
        </section>

        {/* Active Shops */}
        <section class="bg-[#0A0A0A] border border-white/10 rounded-xl p-6">
            <h2 class="text-xl font-bold text-white mb-6">
                Active Shops ({activeShops.length})
            </h2>
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr
                            class="text-white/40 text-xs uppercase tracking-wider border-b border-white/10"
                        >
                            <th class="p-4">Shop</th>
                            <th class="p-4">Owner</th>
                            <th class="p-4">Joined</th>
                            <th class="p-4 text-right">Action</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-white/5">
                        {
                            activeShops.map((shop) => (
                                <tr class="hover:bg-white/5 transition-colors">
                                    <td class="p-4">
                                        <div class="flex items-center gap-3">
                                            <div class="w-8 h-8 rounded-full bg-[#222] overflow-hidden">
                                                {shop.logo_url && (
                                                    <img
                                                        src={shop.logo_url}
                                                        class="w-full h-full object-cover"
                                                    />
                                                )}
                                            </div>
                                            <a
                                                href={`/shop/${shop.slug}`}
                                                target="_blank"
                                                class="text-white font-bold hover:text-[#D4AF37]"
                                            >
                                                {shop.name}
                                            </a>
                                        </div>
                                    </td>
                                    <td class="p-4 text-white/70 text-sm">
                                        {shop.profile?.email}
                                    </td>
                                    <td class="p-4 text-white/50 text-xs">
                                        {new Date(
                                            shop.created_at,
                                        ).toLocaleDateString()}
                                    </td>
                                    <td class="p-4 text-right">
                                        <div class="flex items-center gap-3 justify-end">
                                            <button
                                                data-id={shop.id}
                                                data-action="suspend"
                                                class="action-btn text-orange-500 text-xs hover:underline font-bold"
                                                title="Pause Shop Temporarily"
                                            >
                                                Suspend
                                            </button>
                                            <span class="text-white/10">|</span>
                                            <button
                                                data-id={shop.id}
                                                data-action="delete"
                                                class="action-btn text-red-500 text-xs hover:underline font-bold"
                                                title="Delete Shop Permanently"
                                            >
                                                Delete
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            ))
                        }
                    </tbody>
                </table>
            </div>
        </section>

        {/* Suspended Shops */}
        {
            suspendedShops.length > 0 && (
                <section class="bg-[#0A0A0A] border border-red-500/20 rounded-xl p-6 opacity-80 hover:opacity-100 transition-opacity">
                    <h2 class="text-xl font-bold text-red-500 mb-6 flex items-center gap-2">
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="w-5 h-5"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                        >
                            <>
                                <path d="M18.36 6.64a9 9 0 1 1-12.73 0" />
                                <line x1="12" y1="2" x2="12" y2="12" />
                            </>
                        </svg>
                        Suspended Shops ({suspendedShops.length})
                    </h2>
                    <div class="overflow-x-auto">
                        <table class="w-full text-left border-collapse">
                            <thead>
                                <tr class="text-white/40 text-xs uppercase tracking-wider border-b border-white/10">
                                    <th class="p-4">Shop</th>
                                    <th class="p-4">Owner</th>
                                    <th class="p-4">Status</th>
                                    <th class="p-4 text-right">Action</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-white/5">
                                {suspendedShops.map((shop) => (
                                    <tr class="hover:bg-white/5 transition-colors">
                                        <td class="p-4">
                                            <div class="flex items-center gap-3">
                                                <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center border border-red-500/20 overflow-hidden">
                                                    {shop.logo_url ? (
                                                        <img
                                                            src={shop.logo_url}
                                                            class="w-full h-full object-cover"
                                                        />
                                                    ) : (
                                                        <span class="text-red-500 text-xs font-bold">
                                                            {shop.name[0]}
                                                        </span>
                                                    )}
                                                </div>
                                                <span class="text-white/70 font-bold">
                                                    {shop.name}
                                                </span>
                                            </div>
                                        </td>
                                        <td class="p-4 text-white/50 text-sm">
                                            {shop.profile?.email}
                                        </td>
                                        <td class="p-4">
                                            <span class="text-red-400 text-[10px] font-black uppercase px-2 py-0.5 bg-red-400/10 border border-red-400/20 rounded">
                                                Banned
                                            </span>
                                        </td>
                                        <td class="p-4 text-right">
                                            <div class="flex items-center gap-3 justify-end">
                                                <button
                                                    data-id={shop.id}
                                                    data-action="restore"
                                                    class="action-btn text-green-500 text-xs hover:underline font-bold"
                                                >
                                                    Restore Shop
                                                </button>
                                                <span class="text-white/10">
                                                    |
                                                </span>
                                                <button
                                                    data-id={shop.id}
                                                    data-action="delete"
                                                    class="action-btn text-red-500 text-xs hover:underline font-bold"
                                                >
                                                    Delete
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                ))}
                            </tbody>
                        </table>
                    </div>
                </section>
            )
        }
    </div>
</AdminLayout>

<script>
    document.querySelectorAll(".action-btn").forEach((btn) => {
        btn.addEventListener("click", async (e) => {
            const el = e.currentTarget as HTMLButtonElement;
            const shopId = el.dataset.id;
            const action = el.dataset.action;

            if (!confirm(`Are you sure you want to ${action} this shop?`))
                return;

            el.disabled = true;
            el.innerHTML = "...";

            try {
                const response = await fetch("/api/admin/shop-action", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ shopId, action }),
                });

                const result = await response.json();

                if (!response.ok) {
                    throw new Error(result.error || "Request failed");
                }

                window.location.reload();
            } catch (error) {
                alert("Action failed: " + error.message);
                el.disabled = false;
                el.innerHTML = action; // Reset text
            }
        });
    });
</script>
