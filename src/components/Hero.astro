---
// src/components/Hero.astro
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden pt-20">
    
    <div class="absolute inset-0 z-0">
        <img 
            src="/images/hero-bg.webp" 
            alt="Bitcoin Innovation Devices" 
            class="w-full h-full object-cover opacity-80" 
        />
        
        <div class="absolute inset-0 bg-gradient-to-b from-[#050505] via-[#050505]/30 to-[#050505]"></div>
        
        <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,transparent_20%,#050505_100%)] opacity-80"></div>
    </div>

    <div class="relative z-10 text-center px-6 max-w-6xl mx-auto space-y-8 reveal">
        
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-[#D4AF37]/30 bg-[#D4AF37]/10 backdrop-blur-md mb-4 animate-fade-in-down">
            <span class="w-2 h-2 rounded-full bg-[#D4AF37] animate-pulse"></span>
            <span class="eng text-[#D4AF37] text-xs font-bold uppercase tracking-widest">Innovation for Bitcoiners</span>
        </div>

        <h1 class="text-5xl md:text-7xl lg:text-9xl font-[900] text-white tracking-tighter leading-none uppercase drop-shadow-2xl">
            Bitcoin <br />
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-[#D4AF37] via-[#F4E08F] to-[#D4AF37] animate-gradient">Hardware</span>
        </h1>

        <p class="text-white text-lg md:text-2xl font-light max-w-3xl mx-auto leading-relaxed mt-4 drop-shadow-md">
            ศูนย์รวมนวัตกรรมฝีมือคนไทย: <span class="text-[#D4AF37] font-bold">CryptoClock</span> นาฬิกาดูราคา, 
            <span class="text-[#D4AF37] font-bold">BiTTerm</span> ตู้แลกเหรียญ, และ 
            <span class="text-[#D4AF37] font-bold">BiTNode</span> สำหรับรันโหนดส่วนตัว
        </p>

        <div class="flex flex-col sm:flex-row items-center justify-center gap-4 pt-8">
            <a href="/shop" class="group relative px-8 py-4 bg-[#D4AF37] text-black font-black uppercase tracking-widest rounded-full overflow-hidden transition-all hover:scale-105 active:scale-95 shadow-[0_0_40px_rgba(212,175,55,0.3)] min-w-[200px]">
                <div class="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300"></div>
                <span class="relative flex items-center justify-center gap-2">
                    Shop Now
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                </span>
            </a>
            
            <a href="/shop#kiosk" class="group px-8 py-4 border border-white/20 text-white font-bold uppercase tracking-widest rounded-full hover:bg-white/5 transition-all hover:border-[#D4AF37]/50 min-w-[200px] backdrop-blur-sm">
                <span>See BiTTerm</span>
            </a>
        </div>
    </div>

    <div class="absolute bottom-10 left-1/2 -translate-x-1/2 animate-bounce opacity-80">
        <svg class="w-6 h-6 text-white drop-shadow-md" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/></svg>
    </div>

</section>

<style>
    .animate-gradient {
        background-size: 200% auto;
        animation: shine 4s linear infinite;
    }
    @keyframes shine {
        to { background-position: 200% center; }
    }
    .reveal {
        opacity: 0;
        transform: translateY(30px);
        animation: fadeInUp 1s cubic-bezier(0.19, 1, 0.22, 1) forwards;
        animation-delay: 0.2s;
    }
    @keyframes fadeInUp { to { opacity: 1; transform: translateY(0); } }
</style>