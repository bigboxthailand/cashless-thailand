<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-[#050505] pt-20">
    <div class="absolute inset-0 z-0">
        <div
            class="absolute top-[-10%] left-[-10%] w-[50%] h-[50%] bg-[#D4AF37]/10 blur-[120px] rounded-full animate-pulse">
        </div>
        <div class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-[#D4AF37]/5 blur-[100px] rounded-full"></div>
    </div>

    <div class="max-w-[1800px] mx-auto px-6 md:px-12 w-full grid lg:grid-cols-2 gap-12 items-center relative z-10">

        <div class="space-y-10 order-2 lg:order-1">
            <div class="space-y-4">
                <div class="inline-flex items-center gap-3 px-4 py-2 glass-gold rounded-full reveal"
                    style="animation-delay: 0.2s">
                    <span class="w-2 h-2 bg-[#D4AF37] rounded-full animate-ping"></span>
                    <span class="eng text-[10px] font-black uppercase tracking-[0.3em] text-[#D4AF37]">The New Standard
                        of Wealth</span>
                </div>

                <h1
                    class="text-[clamp(3.5rem,6vw,7rem)] font-black leading-[0.9] tracking-tighter split-text flex flex-col items-start overflow-hidden pb-4">
                    <span>MASTERING</span>
                    <span class="text-gold italic">CASHLESS</span>
                    <span class="text-white/20 eng">ECONOMY.</span>
                </h1>
            </div>

            <p class="text-xl md:text-2xl text-white/40 max-w-lg leading-relaxed font-light reveal"
                style="animation-delay: 0.6s">
                ปลดล็อกศักยภาพการเงินยุคใหม่จาก <span class="text-white font-medium">Bitcoin</span> สู่ <span
                    class="text-white font-medium">Web3</span>
                ครอบครองเครื่องมือและความรู้ที่เหนือกว่าใครในประเทศไทย
            </p>

            <div class="flex flex-wrap gap-6 reveal" style="animation-delay: 0.8s">
                <button
                    class="group relative px-10 py-5 bg-gradient-to-r from-[#996515] to-[#D4AF37] rounded-2xl overflow-hidden transition-all duration-500 hover:scale-105 hover:shadow-[0_20px_40px_-10px_rgba(212,175,55,0.4)]">
                    <div
                        class="absolute inset-0 bg-white/20 translate-y-[100%] group-hover:translate-y-0 transition-transform duration-500">
                    </div>
                    <span class="relative eng font-black text-black text-lg uppercase tracking-wider">Start Learning
                        Now</span>
                </button>

                <button
                    class="group px-10 py-5 border border-white/10 rounded-2xl font-bold text-lg hover:border-[#D4AF37]/50 transition-all flex items-center gap-3 backdrop-blur-sm">
                    <span
                        class="text-white/60 group-hover:text-[#D4AF37] transition-colors uppercase tracking-widest text-sm">Browse
                        Shop</span>
                    <svg class="group-hover:translate-x-2 transition-transform text-[#D4AF37]"
                        xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none"
                        stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M5 12h14m-7-7 7 7-7 7" />
                    </svg>
                </button>
            </div>

            <div class="pt-12 border-t border-white/5 flex gap-12 items-center reveal" style="animation-delay: 1s">
                <div>
                    <div class="eng text-2xl font-black text-white">50K+</div>
                    <div class="text-[10px] uppercase tracking-widest text-white/30">Community Members</div>
                </div>
                <div>
                    <div class="eng text-2xl font-black text-white">100%</div>
                    <div class="text-[10px] uppercase tracking-widest text-white/30">Certified Genuine</div>
                </div>
            </div>
        </div>

        <div class="relative group perspective-1000 hidden lg:block reveal order-1 lg:order-2"
            style="animation-delay: 0.5s">
            <div
                class="absolute inset-0 bg-gradient-to-tr from-[#D4AF37]/20 to-transparent blur-[120px] rounded-full scale-75 group-hover:scale-110 transition-transform duration-1000">
            </div>

            <div id="hero-image-container"
                class="relative z-10 w-full aspect-[4/5] rounded-[4rem] overflow-hidden border border-white/10 shadow-[0_50px_100px_-20px_rgba(0,0,0,1)] bg-gradient-to-b from-[#111] to-black transition-transform duration-700 ease-out"
                style="transform: rotateX(5deg) rotateY(-5deg);">
                <img src="https://images.unsplash.com/photo-1621416894569-0f39ed31d247?q=80&w=1000&auto=format&fit=crop"
                    alt="Premium Hardware Wallet Concept"
                    class="object-cover w-full h-full opacity-90 group-hover:scale-110 transition-transform duration-[2s]" />
                <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-80">
                </div>

                <div class="absolute top-10 left-10">
                    <div class="text-[#D4AF37] text-xs font-black tracking-[0.3em] uppercase mb-2">Flagship Model</div>
                    <div class="text-white text-3xl font-black italic">ULTIMATE SECURITY</div>
                </div>
            </div>

            <div
                class="absolute -bottom-10 -left-10 glass-gold p-6 rounded-3xl z-20 shadow-2xl animate-float backdrop-blur-xl bg-black/60">
                <div class="flex items-center gap-4">
                    <div
                        class="w-12 h-12 bg-black rounded-xl flex items-center justify-center border border-[#D4AF37]/30">
                        <svg class="text-[#D4AF37]" xmlns="http://www.w3.org/2000/svg" width="24" height="24"
                            viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
                        </svg>
                    </div>
                    <div>
                        <div class="eng font-black text-white leading-tight">EAL 6+ Certified</div>
                        <div class="text-[#D4AF37] text-[10px] uppercase tracking-widest font-bold">Military Grade</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    @keyframes float {

        0%,
        100% {
            transform: translateY(0);
        }

        50% {
            transform: translateY(-20px);
        }
    }

    .animate-float {
        animation: float 6s ease-in-out infinite;
    }

    .glass-gold {
        background: rgba(212, 175, 55, 0.05);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(212, 175, 55, 0.1);
    }

    .text-gold {
        background: linear-gradient(to right, #F9E79F, #D4AF37, #996515);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .reveal {
        opacity: 0;
        transform: translateY(40px);
        animation: fadeInUp 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    }

    @keyframes fadeInUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>

<script>
    const container = document.getElementById('hero-image-container');
    if (container) {
        document.addEventListener('mousemove', (e) => {
            const x = (window.innerWidth / 2 - e.clientX) / 25;
            const y = (window.innerHeight / 2 - e.clientY) / 25;
            container.style.transform = `rotateX(${y}deg) rotateY(${-x}deg) translate3d(${x}px, ${y}px, 50px)`;
        });
    }
</script>